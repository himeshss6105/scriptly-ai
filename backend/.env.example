# ============================================================
# Scriptly.AI — Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit the real .env to version control.
# ============================================================

# Port the server runs on
PORT=5000

# ---- MongoDB Atlas ----
# Get this from: https://cloud.mongodb.com
#   Clusters → Connect → Drivers → Node.js
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/scriptly-ai?retryWrites=true&w=majority

# ---- JWT ----
# Generate a strong random string, e.g.: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-this-with-a-long-random-string
JWT_EXPIRES_IN=7d

# ---- Google Gemini API ----
# Get your key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ---- Fair-usage quota ----
# Max generations per user per day against the shared GEMINI_API_KEY above.
# Stops one heavy user from exhausting the daily quota for everyone else.
# Optional — defaults to 10 if not set.
DAILY_QUOTA=10
